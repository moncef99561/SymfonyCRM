{% extends 'base.html.twig' %}

{% block title %}Ajouter une facture{% endblock %}

{% block body %}
<h1>Ajouter une facture à {{ client.raisonSocial }}</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Enregistrer</button>
{{ form_end(form) }}

{% endblock %}
